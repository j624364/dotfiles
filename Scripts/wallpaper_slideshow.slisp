#!/usr/bin/env slisp

(define secs-in-minute 60)
(define sleep-time (× secs-in-minute 2))

(while true (
	(sleep sleep-time)
	(system-raw "nitrogen --set-zoom-fill --random ~/Backgrounds/slideshow --save")
))

